package mx.moa.miabarroteria.model
/*
import com.google.mlkit.vision.barcode.BarcodeScannerOptions
import com.google.mlkit.vision.barcode.common.Barcode.FORMAT_UPC_A

val options = BarcodeScannerOptions.Builder()
    .setBarcodeFormats(FORMAT_UPC_A)
    .setZoomSuggestionOptions(
        new ZoomSuggestionOptions.Builder(setZoom)
            .setMaxSupportedZoomRatio(maxSupportedZoomRatio)
            .build())
    .build()


fun setZoom(ZoomRatio: Float): Boolean {
    if (camera.isClosed()) return false
    camera.getCameraControl().setZoomRatio(zoomRatio)
    return true
}
*/